<% if @list.valid? %>
	$("#<%= dom_id @list, :public %>").slideUp( function(){ $(this).remove(); } );
	$("#<%= dom_id @list, :private %>").find("#publish_button").text('Publicar').attr('href', '<%=publish_list_path(@list)%>');
<% end %>
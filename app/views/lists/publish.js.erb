<% if @list.valid? %>
	var partial = "<%= j(render 'public_list', public_list: @list) %>";
	$('#public_lists_table').append(partial);
	$("#<%= dom_id @list, :private %>").find("#publish_button").text('Ocultar').attr('href', '<%=unpublish_list_path(@list)%>');
<% end %>